<!DOCTYPE html>
<html lang="en">
<head>
{% block head %}
    <meta charset="UTF-8">
    <title>Title</title>
    <link rel="stylesheet" href="/uikit/css/uikit.almost-flat.min.css">
    <link rel="stylesheet" href="/css/main.css">
    <script type="text/javascript" src="/js/jquery-3.1.0.min.js"></script>
    <script type="text/javascript" src="/uikit/js/uikit.min.js"></script>
    <script type="text/javascript" src="/js/main.js"></script>
{% endblock %}
</head>
<body data-refresh="1000">
    <div class="uk-container uk-container-center uk-margin-top">
        <nav class="uk-navbar">
            <a href="" class="uk-navbar-brand">
                <i class="uk-icon-stethoscope"></i>
                PlantHero
            </a>
            <div class="uk-navbar-content uk-text-small"> - <i>"Should I wet the plant?"</i></div>
            <div class="uk-navbar-content uk-navbar-flip uk-text-primary">
                <ul class="uk-navbar-nav">
                    <li>
                        <a href="#" class="uk-navbar-nav-subtitle" id="wd-top">
                            <span>{{ app.recommender.getWeatherDescription }}</span> today
                            <div>weather description</div>
                        </a>
                    </li>
                    <li>
                        <a href="#" class="uk-navbar-nav-subtitle" id="tagh-top">
                            <span>{{ app.recommender.todayAverageGroundHumidity | round }}</span> today
                            <div>avg ground humidity</div>
                        </a>
                    </li>
                    <li>
                        <a href="#" class="uk-navbar-nav-subtitle" id="tat-top">
                            <span>{{ app.recommender.todayAverageTemperature | round }}</span> C today
                            <div>avg temperature</div>
                        </a>
                    </li>
                    {% if app.recommender.yesterdayAverageGroundHumidity is not empty %}
                        <li>
                            <a href="#" class="uk-navbar-nav-subtitle" id="yagh-top">
                                <span>{{ app.recommender.yesterdayAverageGroundHumidity | round }}</span> yesterday
                                <div>avg ground humidity</div>
                            </a>
                        </li>
                    {% endif %}
                    {% if app.recommender.yesterdayAverageTemperature is not empty %}
                        <li>
                            <a href="#" class="uk-navbar-nav-subtitle" id="yat-top">
                                <span>{{ app.recommender.yesterdayAverageTemperature | round }}</span> yesterday
                                <div>avg temperature</div>
                            </a>
                        </li>
                    {% endif %}
                </ul>
            </div>
        </nav>
        <br>
        {% block body %}
        {% endblock %}
    </div>
</body>
</html>
